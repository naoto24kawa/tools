name = "tools-roman-numeral"
compatibility_date = "2024-10-19"

# Cloudflare Pages Configuration
# Deploy: wrangler pages deploy dist --project-name=tools-roman-numeral
# Build output directory: dist
# Framework: React + Vite
