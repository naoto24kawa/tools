name = "tools-network-port-reference"
compatibility_date = "2024-10-19"

# Cloudflare Pages Configuration
# Deploy: wrangler pages deploy dist --project-name=tools-network-port-reference
# Build output directory: dist
# Framework: React + Vite
