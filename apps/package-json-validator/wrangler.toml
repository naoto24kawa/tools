name = "tools-package-json-validator"
compatibility_date = "2024-10-19"

# Cloudflare Pages Configuration
# Deploy: wrangler pages deploy dist --project-name=tools-package-json-validator
# Build output directory: dist
# Framework: React + Vite
