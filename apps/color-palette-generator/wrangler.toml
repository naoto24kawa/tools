name = "tools-color-palette-generator"
compatibility_date = "2024-10-19"

# Cloudflare Pages Configuration
# Deploy: wrangler pages deploy dist --project-name=tools-color-palette-generator
# Build output directory: dist
# Framework: React + Vite
